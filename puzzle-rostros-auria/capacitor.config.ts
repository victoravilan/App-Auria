import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.auria.puzzlerostros',
  appName: 'Puzzle Rostros Auria',
  webDir: 'dist'
};

export default config;
